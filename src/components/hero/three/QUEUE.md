# 3D build queue

## Current: finish the v2 build

Complete the current implementation and its remaining acceptance checks before
starting the next prompt. Track remaining work and verified limitations in
README.md. Do not treat unavailable physical-device testing as passed.

## Pending: v3 scene alignment, process stills, record diagram

Status: queued, not started.

Full supplied prompt: [queue/v3-prompt.md](queue/v3-prompt.md).

Jay explicitly requested that this prompt wait until the current build is
finished. Saving it here does not activate its implementation instructions or
authorize changes to the current build based on v3 requirements.
